<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Month_Next_Month__c</fullName>
    <externalId>false</externalId>
    <formula>IF(MONTH( TODAY()) =12, 1, 
IF(MONTH( TODAY()) = 1, 2, 
IF(MONTH( TODAY()) = 2, 3,
IF(MONTH( TODAY()) = 3, 4,
IF(MONTH( TODAY()) = 4, 5,
IF(MONTH( TODAY()) = 5, 6,
IF(MONTH( TODAY()) = 6, 7,
IF(MONTH( TODAY()) = 7, 8,
IF(MONTH( TODAY()) = 8, 9,
IF(MONTH( TODAY()) = 9, 10,
IF(MONTH( TODAY()) = 10, 11,
IF(MONTH( TODAY()) = 11, 12,
0))))))))))))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Month - Next Month</label>
    <precision>18</precision>
    <required>false</required>
    <scale>0</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
